name: flutter_wordpress_starter
description: 🚀 The ultimate Flutter starter template for WordPress-powered apps. Clone, configure, and deploy in under 5 minutes.

# The following defines the version and build number for your application.
version: 2.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
    
  # State Management
  provider: ^6.1.1
  
  # Network & Caching (simplified)
  http: ^1.2.0
  cached_network_image: ^3.3.1
  connectivity_plus: ^5.0.2
  
  # Storage (simplified - removed SQLite)
  shared_preferences: ^2.2.2
  
  # UI Components (essential only)
  flutter_html: ^3.0.0-beta.2
  infinite_scroll_pagination: ^4.1.0
  
  # Utils (minimal)
  intl: ^0.20.2
  cupertino_icons: ^1.0.6
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
  # - images/a_dot_burr.jpeg
    - assets/images/placeholder.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.io/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.io/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: NotoKufiArabic
      fonts:
        - asset: lib/src/fonts/NotoKufiArabic-Regular.ttf
    - family: NotoSansArabic
      fonts:
        - asset: lib/src/fonts/NotoSansArabic-Regular.ttf
          weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.io/custom-fonts/#from-packages
